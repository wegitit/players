# per help.github.com/articles/generating-ssh-keys/#platform-linux

# generate ssh key
ssh-keygen -C "theClayTyler@gmail.com"

# prompt for passphrase
# BeneGesserit*

# enter passphrase again

# add ssh key to ssh-agent
#  start the ssh-agent in the background
$ eval "$(ssh-agent -s)"
# should return "Agent pid ####"
#ssh-add ~/.ssh/id_rsa

